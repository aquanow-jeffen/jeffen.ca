import type { NextPage } from "next";
import PageLayout from "@components/pageLayout";

const Resume: NextPage = () => {
  return <PageLayout title="Resume">Resume</PageLayout>;
};

export default Resume;
